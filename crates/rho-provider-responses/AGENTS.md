# Agent instructions

Read `ARCHITECTURE.md` before changing or reviewing this crate's provider
protocol, public API, auth, request-building, streaming, or WebSocket-pooling
behavior.

Before changing this crate, read `SECURITY.md` in this directory and the
repository root `SECURITY.md`.
