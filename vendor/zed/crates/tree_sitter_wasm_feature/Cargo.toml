[package]
name = "tree-sitter-wasm-feature"
version = "0.1.0"
edition.workspace = true
publish = false
license = "GPL-3.0-or-later"

[lib]
path = "src/lib.rs"

[dependencies]
tree-sitter = { workspace = true, features = ["wasm"] }
